{
  "name": "aisstream-viewer",
  "version": "1.0.0",
  "description": "Local AIS vessel data viewer via AISStream.io",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "tsx --env-file=.env src/server.ts",
    "db:seed-mpas": "tsx --env-file=.env scripts/import-mpas.ts"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/pg": "^8.11.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
